# Local AI Binaries

This folder contains the pre-compiled binaries for the local AI engine (llama-server).
For macOS (Apple Silicon): llama-server
For Windows (x64): llama-server.exe

During build time, these files are copied into the final app package.
